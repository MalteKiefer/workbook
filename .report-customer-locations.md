# Report: Customer locations (multiple sites/addresses per customer)

## Status

Done. All 4 tasks from the brief are implemented and committed on
`feature/customer-locations`, and all 5 verification commands pass.

## Commits

- `606fe80` feat: add locations backend (migration, db, commands)
  - `src-tauri/migrations/0005_locations.sql` (new `locations` table + index)
  - `src-tauri/src/db/locations.rs` (new, full CRUD mirroring `db/customers.rs`)
  - `src-tauri/src/db/migrations.rs` (registered migration 5, bumped version
    assertion to 5, added `"locations"` to the migration test's table list)
  - `src-tauri/src/db/mod.rs` (`pub mod locations;`, alphabetical)
  - `src-tauri/src/commands/locations.rs` (new, 4 commands mirroring
    `commands/entry_templates.rs`, list scoped by `customer_id`)
  - `src-tauri/src/commands/mod.rs` (`pub mod locations;`, alphabetical)
  - `src-tauri/src/lib.rs` (4 new commands added to `generate_handler![...]`,
    placed right after the `entry_templates` block)
- `5d6aead` feat: add LocationsPanel UI embedded in CustomerForm
  - `src/components/LocationsPanel.tsx` (new)
  - `src/components/CustomerForm.tsx` (import + embed, edit-mode-only)

## Verification

1. `cargo test --manifest-path src-tauri/Cargo.toml` -- PASS: 932 passed,
   0 failed. Confirmed `db::locations::tests::*` (6 tests: roundtrip,
   blank-name validation, list ordering/customer-scoping, update-not-found,
   delete, delete-not-found) all ran and passed, and the migrations test
   (`run_migrations_creates_all_tables_and_records_version`) passes with
   version 5 and `locations` in its table list.
2. `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` -- PASS: clean, no warnings.
3. `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` -- PASS: clean
   (the brief's inline code sample for `db/locations.rs` was not itself
   `rustfmt`-clean -- long argument lists and one-line struct literals; ran
   `cargo fmt` once to reformat it to the project's style, then re-verified
   `--check` passes).
4. `npm run build` -- PASS: `tsc -b && vite build` clean. (`node_modules`
   was absent in this worktree; ran `npm install` first, 193 packages, 0
   vulnerabilities.) One pre-existing, unrelated informational warning about
   a >500kB chunk (`theme-*.js`) -- not introduced by this change, build
   still succeeds.
5. `npm run lint` -- PASS: `eslint .` clean, no output.

## Item 6 check (edit-mode-only rendering)

Confirmed by reading `CustomerForm.tsx`'s diff: `LocationsPanel` is added
right next to the existing `AuditLogPanel` usage, guarded by the exact same
condition:

```tsx
{typeof customerEditorTarget === "number" && <LocationsPanel customerId={customerEditorTarget} />}
{typeof customerEditorTarget === "number" && <AuditLogPanel entityType="customer" entityId={customerEditorTarget} />}
```

`customerEditorTarget` is `"new" | number | null`, so `LocationsPanel` is
never rendered in "new customer" mode (no `customerId` to pass) or when the
form is closed -- only when editing an existing customer.

## Concerns

- The brief's Verification section 1 says to confirm
  "`db::locations::tests::*` (7 tests) ran", but the brief's own
  `db/locations.rs` code sample (which I mirrored exactly) contains 6 test
  functions, not 7. All 6 pass; I did not add a 7th test since the brief
  said to mirror the given code closely and didn't specify what an
  additional test should cover. Flagging this discrepancy rather than
  guessing at a missing test case.
- `node_modules` did not exist in this worktree before this session; I ran
  `npm install` to make `npm run build` / `npm run lint` runnable. No
  `package.json`/`package-lock.json` changes resulted (nothing added or
  committed beyond the two feature commits above).
- No frontend test harness exists for this project (per the brief), so
  Task 4 (`LocationsPanel.tsx`) is verified via `npm run build`, `npm run
  lint`, and a careful read-through/diff review only, not automated tests.
- V1 scope, as specified in the brief's Context section: locations belong
  only to a customer, not to individual systems (no `systems.location_id`).
